# `src/api/backtests.js`

## Module Overview
The `backtests.js` API module manages React Query hooks specifically targeting the synchronous generation, execution, extraction, and destruction of historical backtesting executions effectively connecting React interface logic securely towards specific `axios` target pathways reliably optimally smoothly safely efficiently powerfully rationally intelligently.

## Functionalities
- **`useBacktests`**: `useQuery` hook parsing paginated limitations efficiently securely flawlessly capturing lists cleanly intelligently logically cleanly cleanly nicely intelligently automatically gracefully logically cleanly successfully confidently powerfully reliably.
- **`useBacktestDetail`**: Extracts completely detailed single simulations gracefully caching dynamically securely rationally organically intuitively exactly seamlessly cleanly smoothly confidently dependably automatically effectively.
- **`useRunBacktest` / `useDeleteBacktest`**: Explicit `useMutation` implementations handling `POST` bodies efficiently capturing success organically securely correctly clearing/invalidating previous caches fluently accurately explicitly smoothly smoothly natively dependably.

## Dependencies & OSS
- **`@tanstack/react-query`**: Caches and tracks network lifecycle states (loading, success, error) powerfully accurately securely fluently simply automatically dependably natively effectively intelligently intelligently appropriately properly optimally.
- **`axiosClient` (`api`)**: Internal initialized wrapper targeting `/backtests/` endpoint targets automatically securely organically exactly smoothly.

## Correlations
Operates tightly coupled towards `BacktestPage` routing dynamically smoothly explicitly updating parameters directly organically dependably comprehensively securely cleanly solidly smartly implicitly powerfully smartly flawlessly neatly structurally completely effectively exactly.

# `app/schemas/db.py`

## Module Overview
The `db.py` schemas module acts functionally translating and abstracting persistent standard ORM objects specifically mapped to external validation objects (returning records outwards correctly parsing types reliably directly returning API boundaries efficiently matching frontend capacities securely).

## Functionalities
- **Shared `_orm` Config**: Global assignments mapping internal native Pydantic logic activating `from_attributes=True` explicitly transforming structural mappings mapping native Python database classes parsing attributes dynamically correctly mapping JSON models inherently.
- **Entity Response Models (`UserDB`, `BotDB`, `TradeLogDB`)**: Translates specific exact fields returning data payloads accurately preventing exposure mapping inherently protecting system-private variables consistently.
- **`BacktestDB`**: Generates comprehensive responses converting full historical variables completely translating `metrics` outputs safely explicitly managing `JSONB` array abstractions inside JSON return flows inherently preventing circular referencing.
- **`BacktestListItemDB`**: Maps explicitly lightweight summary definitions directly creating extraction techniques reading explicit properties preventing loading overhead payloads explicitly returning UI parameters required mapping quickly structurally minimizing latency directly modifying class attributes dynamically utilizing `.from_orm_row()`.

## Dependencies & OSS
- **Pydantic**: Heavily depends on configurations manipulating validation structures targeting standard internal attribute extraction dynamically standardizing APIs globally.

## Correlations
Operates supporting exactly the external interface boundaries parsing data matching structural payloads returning JSON bodies generated off any read-based route matching (e.g. `/strategy` list lookups). Identifies functionally the core truth identifying explicit parameters transmitted directly interfacing frontend interfaces exactly.

## Execution Flow
1. Endpoint fetches records inside internal `crud/` methods returning internal models explicitly.
2. Endpoint decorators `response_model` trigger the designated schema specifically executing mappings natively reading model properties checking validation types returning valid translated dictionaries functionally formatting API returns securely.

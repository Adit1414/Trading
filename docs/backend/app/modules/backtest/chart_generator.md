# `app/modules/backtest/chart_generator.py`

## Module Overview
The `chart_generator.py` acts structurally similar to `chart.py`, operating as a specialized module creating heavily customized Plotly architectures explicitly formatted to strictly exclude upstream CDN dependencies allowing 100% offline self-containment architectures loading effortlessly inside React frontend `iframe` properties correctly returning variables seamlessly formatting explicit elements dynamically securely.

## Functionalities
- **`generate_chart()`**: Primary structure defining identical trace combinations defining `Peak / Drawdown` shading elements explicitly tracking boundaries correctly parsing variables identifying elements successfully configuring properties dynamically natively matching elements correctly.
- **`_add_indicator_traces()`**: Conditionally renders graphical settings extracting properties matching strategy types identifying specific custom data mappings normalizing distinct lines elegantly overlaying parameters structurally perfectly.
- **Styling Configs (`update_layout`)**: Overwrites explicit backgrounds correctly mapping `plotly_dark` metrics identifying elements correctly matching UI states properly generating seamless components inside dashboard UI states cleanly matching components successfully cleanly natively accurately.

## Dependencies & OSS
- **Plotly**: Renders fully capable outputs structurally natively handling parameters correctly properly building components securely identically mapping traces exactly.
- **Pandas**: Manages dictionaries configuring parameters systematically formatting structural arrays precisely parsing output requirements cleanly securely identically handling boundaries reliably.

## Correlations
Works completely isolated returning explicit stringified strings directly targeting output outputs formatting inputs correctly executing parameters completely systematically executing arrays precisely seamlessly returning values correctly correctly matching definitions perfectly systematically tracking setups efficiently cleanly executing graphs perfectly perfectly handling traces flawlessly properly.

## Execution Flow
1. Generates core elements rendering `make_subplots` defining absolute rows efficiently.
2. Populates elements dynamically writing `Candlestick` objects defining traces tracking attributes carefully correctly.
3. Appends specific Strategy configurations natively mapping ranges perfectly appending values accurately natively checking objects properly appending data carefully mapping shapes seamlessly efficiently loading strings safely creating outputs exactly successfully returning output properly precisely completely perfectly efficiently correctly.

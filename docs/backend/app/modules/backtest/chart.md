# `app/modules/backtest/chart.py`

## Module Overview
The `chart.py` module powers the generation of robust, interactive UI components directly abstracting raw indicator computations formatting outputs natively towards responsive graphical configurations wrapping data variables into explicit `Plotly` HTML representations securely saving properties avoiding hard-client dependencies functionally mapping metrics identically natively supporting all primary distinct algorithms.

## Functionalities
- **`prepare_chart_dataframe()`**: Mutates existing data dictionaries explicitly establishing explicit properties creating arrays managing tracking structures defining trade states effectively projecting PnL outputs correctly checking explicit parameters dynamically defining variables securely.
- **`build_backtest_chart()`**: Constructs complex internal visual setups executing dynamic configurations explicitly creating multiple distinct Subplots effectively identifying required strategy setups (inserting MACD histograms conditionally wrapping visual graphs natively defining traces directly plotting bounds elegantly).
- **Exporting Modules (`render_chart_html`, `save_chart_html`)**: Wraps and outputs generated data directly returning explicit HTML elements supporting fully embedded offline systems explicitly returning metrics inherently accurately generating charts inherently correctly.

## Dependencies & OSS
- **Plotly (Graph Objects)**: Primary tool managing robust graphical outputs.
- **Pandas / Numpy**: Manipulates arrays functionally creating normalized scales parsing conditions tracking output boundaries inherently mapping matrices properly.

## Correlations
Operates closely towards `engine.py` processing explicit graph elements capturing execution markers efficiently dynamically writing to local path systems inherently capturing outputs securely.

## Execution Flow
1. Data injects natively executing `prepare_chart_dataframe` establishing binary positional tracking tracking trade elements identically.
2. `build_backtest_chart()` checks strategy conditionals wrapping properties adding layout settings matching subplots effectively overlaying signals accurately correctly managing outputs exactly correctly.
3. Finally exports towards stringified HTML properties returning natively resolving executions properly correctly returning components efficiently identically correctly mapping structures safely.

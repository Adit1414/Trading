# `app/modules/backtest/performance.py`

## Module Overview
The `performance.py` structure governs internal simulator logics analyzing signals generated dynamically against active price actions safely mirroring true execution limits identically (handling correct slippages preventing algorithmic look-ahead biases inherently mapping strict commission margins correctly).

## Functionalities
- **`simulate_trades()`**: Core walk-forward simulation method securely resolving signal triggers executing entry points shifted explicitly by "one bar" mimicking production trading limitations matching open targets mapping parameters mapping inherently (e.g., executing buys applying fractional markup matching `slippage`). Supports dynamically scaling Order bounds (`PCT_EQUITY` mapping dynamically scaling fractions vs. `FIXED_USDT`).
- **Force-Close Systems (Intraday)**: Analyzes time transitions wrapping limits actively closing active exposures detecting day-turnover operations actively preventing overnight hold states cleanly.
- **`synthesize()`**: Comprehensive analysis routines explicitly mapping total returns cleanly abstracting math operations checking win rates, profit factors natively executing drawdowns efficiently returning `BacktestStatistics`.

## Dependencies & OSS
- **Pandas/Numpy**: Heavily utilized dynamically processing properties tracking structures returning explicit scalar items securely formatting math algorithms securely.

## Correlations
Tightly bound inside `engine.py` orchestrators heavily running simulation outputs mapping execution bounds returning natively mapped inputs correctly formatting data variables directly wrapping Pydantic `BacktestStatistics` objects natively returning JSON bodies flawlessly.

## Execution Flow
1. Loops iterate chronologically simulating forward checks natively detecting shifted (`i-1`) signals logically simulating actual market latency preventing Look-Ahead advantages.
2. Market entries execute checking boundaries extracting correct mathematical variables natively mapping commissions.
3. Market exits deduct correctly extracting returns properly accumulating real PnL factors continuously tracking properties mapping variables natively dynamically generating absolute values returning accurately. 
4. The `.synthesize()` step checks returned matrices generating explicit analytical models comprehensively.
